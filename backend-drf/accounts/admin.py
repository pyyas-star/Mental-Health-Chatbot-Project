"""
Django Admin configuration for Accounts app.

User authentication models are managed by Django's built-in admin.
No custom admin configuration needed.
"""

from django.contrib import admin

# User model is registered by Django's built-in admin
# No additional models to register here

No custom admin configuration needed.
"""

from django.contrib import admin

# User model is registered by Django's built-in admin
# No additional models to register here

No custom admin configuration needed.
"""

from django.contrib import admin

# User model is registered by Django's built-in admin
# No additional models to register here

No custom admin configuration needed.
"""

from django.contrib import admin

# User model is registered by Django's built-in admin
# No additional models to register here
